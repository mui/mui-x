{
  "componentDescription": "",
  "propDescriptions": {
    "alwaysVisible": {
      "description": "By default, suggestions only render when the active thread has no messages (treating them as an empty-state affordance). Set to <code>true</code> to render the suggestions regardless of message count — e.g. as a &quot;next prompt&quot; row above the composer in an active conversation."
    },
    "autoSubmit": { "description": "Whether to auto-submit when a suggestion is clicked." },
    "suggestions": {
      "description": "Suggestion items. Strings are normalized to <code>{ value, label }</code>. Ignored when <code>children</code> are provided."
    }
  },
  "classDescriptions": {
    "item": {
      "description": "Styles applied to {{nodeName}}.",
      "nodeName": "each suggestion item"
    },
    "root": { "description": "Styles applied to the root element." }
  }
}
