{
  "componentDescription": "",
  "propDescriptions": {
    "angle": {
      "description": "The angle (in degrees) along which the tick labels are rendered. By default, uses the start angle of the rotation axis."
    },
    "axisId": {
      "description": "Id of the radius axis to render. If not provided, it will use the first defined radius axis."
    },
    "className": { "description": "A CSS class name applied to the root element." },
    "disableLine": { "description": "If <code>true</code>, the axis line is not rendered." },
    "disableTicks": { "description": "If <code>true</code>, the ticks are not rendered." },
    "tickLabelPosition": {
      "description": "Set the position of the tick labels relative to the axis line. The before/after is defined based on clockwise direction."
    },
    "tickPosition": {
      "description": "Set the position of the tick relative to the axis line. The before/after is defined based on clockwise direction."
    },
    "tickSize": { "description": "The size (in pixels) of the tick marks." }
  },
  "classDescriptions": {}
}
