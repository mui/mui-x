{
  "componentDescription": "",
  "propDescriptions": {
    "axis": { "description": "" },
    "axisId": {
      "description": "The id of the axis to render.\nIf undefined, it will be the first defined axis."
    },
    "classes": { "description": "Override or extend the styles applied to the component." },
    "disableLine": { "description": "If true, the axis line is disabled." },
    "disableTickLabel": { "description": "If true, the tick labels are not rendered." },
    "disableTicks": { "description": "If true, the ticks are disabled." },
    "endAngle": { "description": "The end angle (in deg)." },
    "label": { "description": "The label of the axis." },
    "labelGap": { "description": "The gap between the axis and the label." },
    "labelStyle": { "description": "The style applied to the axis label." },
    "position": {
      "description": "The position of the rotation axis.\nIt can be 'inside' or 'outside'."
    },
    "startAngle": { "description": "The start angle (in deg)." },
    "tickInterval": {
      "description": "Defines which ticks are displayed.\nIts value can be:\n- 'auto' In such case the ticks are computed based on axis scale and other parameters.\n- a filtering function of the form <code>(value, index) => boolean</code> which is available only if the axis has \"point\" scale.\n- an array containing the values where ticks should be displayed.",
      "seeMoreText": "See {{link}} for more details."
    },
    "tickLabelInterval": {
      "description": "Defines which ticks get its label displayed. Its value can be:\n- 'auto' In such case, labels are displayed if they do not overlap with the previous one.\n- a filtering function of the form (value, index) => boolean. Warning: the index is tick index, not data ones."
    },
    "tickLabelPlacement": {
      "description": "The placement of ticks label. Can be the middle of the band, or the tick position.\nOnly used if scale is 'band'."
    },
    "tickLabelPosition": {
      "description": "Set the position of the tick labels relative to the axis line.\n<code>'after'</code> places them outside the arc, <code>'before'</code> inside."
    },
    "tickLabelStyle": { "description": "The style applied to ticks text." },
    "tickMaxStep": {
      "description": "Maximal step between two ticks.\nWhen using time data, the value is assumed to be in ms.\nNot supported by categorical axis (band, points)."
    },
    "tickMinStep": {
      "description": "Minimal step between two ticks.\nWhen using time data, the value is assumed to be in ms.\nNot supported by categorical axis (band, points)."
    },
    "tickNumber": {
      "description": "The number of ticks. This number is not guaranteed.\nNot supported by categorical axis (band, points)."
    },
    "tickPlacement": {
      "description": "The placement of ticks in regard to the band interval.\nOnly used if scale is 'band'."
    },
    "tickPosition": {
      "description": "Set the position of the tick relative to the axis line.\n<code>'after'</code> places them outside the arc, <code>'before'</code> inside."
    },
    "tickSize": { "description": "The size of the ticks." },
    "tickSpacing": {
      "description": "The minimum space between ticks when using an ordinal scale. It defines the minimum distance in pixels between two ticks."
    }
  },
  "classDescriptions": {},
  "slotDescriptions": {}
}
