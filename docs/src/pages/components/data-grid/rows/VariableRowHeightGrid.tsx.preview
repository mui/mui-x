<DataGrid
  rows={rows}
  columns={columns}
  getRowHeight={({ id }: GridRowHeightParams) => {
    if ((id as number) % 2 === 0) {
      return 100;
    }

    return null;
  }}
  components={{
    Toolbar: CustomToolbar,
  }}
/>