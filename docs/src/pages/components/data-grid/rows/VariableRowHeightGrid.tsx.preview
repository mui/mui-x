<DataGrid
  {...data}
  getRowHeight={() => {
    isRowIndexOdd.current = !isRowIndexOdd.current;

    if (isRowIndexOdd.current) {
      return 100;
    }

    return null;
  }}
  components={{
    Toolbar: CustomToolbar,
  }}
/>