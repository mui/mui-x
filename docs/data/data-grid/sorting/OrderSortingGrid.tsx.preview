<DataGrid
  sortingOrder={['desc', 'asc']}
  initialState={{
    ...data.initialState,
    sorting: {
      ...data.initialState?.sorting,
      sortModel: [
        {
          field: 'commodity',
          sort: 'asc',
        },
      ],
    },
  }}
  {...data}
/>