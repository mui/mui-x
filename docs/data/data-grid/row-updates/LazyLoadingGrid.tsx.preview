<DataGridPro
  columns={columns}
  rows={cachedRows}
  apiRef={apiRef}
  hideFooterPagination
  rowCount={ROW_COUNT}
  sortingMode="server"
  filterMode="server"
  rowsLoadingMode="server"
  onFetchRows={handleFetchRows}
  experimentalFeatures={{
    lazyLoading: true,
  }}
/>