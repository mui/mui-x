<DataGrid
  {...data}
  getRowSpacing={getRowSpacing}
  sx={{
    [`& .${dataGridClasses.row}`]: {
      bgcolor: (theme) =>
        theme.palette.mode === 'light' ? grey[200] : grey[900],
    },
  }}
/>