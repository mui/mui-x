<DataGridPremium
  treeData
  rows={rows}
  columns={columns}
  getTreeDataPath={getTreeDataPath}
  getRowId={getRowId}
  groupingColDef={groupingColDef}
  initialState={{
    aggregation: {
      model: {
        size: 'sum',
        updatedAt: 'max',
      },
    },
  }}
/>