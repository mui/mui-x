<DataGridPremium
  {...data}
  loading={loading}
  slots={{ toolbar: CustomToolbar }}
  initialState={{
    ...data.initialState,
    rowGrouping: {
      ...data.initialState?.rowGrouping,
      model: ['commodity'],
    },
  }}
/>