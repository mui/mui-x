<PieChart
  series={[
    {
      outerRadius: 80,
      data,
      arcLabel: (params) => {
        const percent = params.value / TOTAL;
        return `${(percent * 100).toFixed(0)}%`;
      },
    },
  ]}
  margin={{ right: 5 }}
  width={200}
  height={200}
  legend={{ hidden: true }}
/>