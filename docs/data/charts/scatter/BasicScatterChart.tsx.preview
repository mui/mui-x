<ScatterChart
  width={600}
  height={400}
  series={[
    {
      id: 's1',
      markerSize: 5,
      data: data.map(({ x1, y1, id }) => ({ x: x1, y: y1, id })),
    },
    {
      id: 's2',
      data: data.map(({ x1, y2, id }) => ({ x: x1, y: y2, id })),
    },
  ]}
/>