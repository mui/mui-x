<LineChartPro
  width={600}
  height={300}
  zoom
  series={[
    {
      label: 'Series A',
      data: data.map((v) => v.y1),
    },
    {
      label: 'Series B',
      data: data.map((v) => v.y2),
    },
  ]}
/>