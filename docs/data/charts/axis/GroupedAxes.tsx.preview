<BarChart
  xAxis={[
    {
      data,
      scaleType: 'band',
      tickSize: 8,
      height: 32,
      groups: [
        {
          getValue: (v) => v.toLocaleDateString('en-US', { month: 'short' }),
        },
        { getValue: getQuarter },
        { getValue: getYear },
      ],
      valueFormatter,
    },
  ]}
  {...chartConfig}
/>