<BarChart
  {...settings}
  onHighlightChange={setHighlightedItem}
  sx={
    highlightedItem
      ? {
          [`.${legendClasses.item}:not([data-series="${highlightedItem.seriesId}"])`]:
            {
              opacity: 0.4,
            },
        }
      : undefined
  }
/>