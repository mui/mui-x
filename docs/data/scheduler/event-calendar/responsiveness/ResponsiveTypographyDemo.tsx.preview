<div
  style={{
    height: '500px',
    width: '100%',
    maxWidth: MAX_WIDTHS[size],
  }}
>
  <StandaloneWeekView
    events={events}
    resources={resources}
    defaultVisibleDate={defaultVisibleDate}
    onEventsChange={setEvents}
  />
</div>
