import { HeatmapPlot } from '@mui/x-charts-pro/Heatmap';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesHeatmapPlot = createTypes(import.meta.url, HeatmapPlot);
