import { ChartPrintExportOptions } from '@mui/x-charts-pro';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChartPrintExportOptions = createTypes(import.meta.url, ChartPrintExportOptions);
