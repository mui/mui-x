import { ChartsTooltipContainer } from '@mui/x-charts/ChartsTooltip';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChartsTooltipContainer = createTypes(import.meta.url, ChartsTooltipContainer);
