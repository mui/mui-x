import { FunnelChart } from '@mui/x-charts-pro/FunnelChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesFunnelChart = createTypes(import.meta.url, FunnelChart);
