import { RadarSeriesMarks } from '@mui/x-charts/RadarChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesRadarSeriesMarks = createTypes(import.meta.url, RadarSeriesMarks);
