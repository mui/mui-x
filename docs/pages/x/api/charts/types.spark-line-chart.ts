import { SparkLineChart } from '@mui/x-charts/SparkLineChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesSparkLineChart = createTypes(import.meta.url, SparkLineChart);
