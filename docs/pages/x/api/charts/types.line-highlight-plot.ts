import { LineHighlightPlot } from '@mui/x-charts/LineChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesLineHighlightPlot = createTypes(import.meta.url, LineHighlightPlot);
