import { FocusedSankeyNode } from '@mui/x-charts-pro/SankeyChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesFocusedSankeyNode = createTypes(import.meta.url, FocusedSankeyNode);
