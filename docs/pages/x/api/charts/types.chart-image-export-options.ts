import { ChartImageExportOptions } from '@mui/x-charts-pro';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChartImageExportOptions = createTypes(import.meta.url, ChartImageExportOptions);
