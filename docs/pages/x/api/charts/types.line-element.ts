import { LineElement } from '@mui/x-charts/LineChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesLineElement = createTypes(import.meta.url, LineElement);
