import { MarkElement } from '@mui/x-charts/LineChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesMarkElement = createTypes(import.meta.url, MarkElement);
