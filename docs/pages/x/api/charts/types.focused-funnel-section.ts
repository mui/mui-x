import { FocusedFunnelSection } from '@mui/x-charts-pro/FunnelChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesFocusedFunnelSection = createTypes(import.meta.url, FocusedFunnelSection);
