import { SankeyTooltip } from '@mui/x-charts-pro/SankeyChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesSankeyTooltip = createTypes(import.meta.url, SankeyTooltip);
