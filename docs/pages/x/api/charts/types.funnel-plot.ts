import { FunnelPlot } from '@mui/x-charts-pro/FunnelChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesFunnelPlot = createTypes(import.meta.url, FunnelPlot);
