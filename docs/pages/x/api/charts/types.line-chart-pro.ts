import { LineChartPro } from '@mui/x-charts-pro/LineChartPro';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesLineChartPro = createTypes(import.meta.url, LineChartPro);
