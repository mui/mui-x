import { ChartsTooltip } from '@mui/x-charts/ChartsTooltip';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChartsTooltip = createTypes(import.meta.url, ChartsTooltip);
