import { AnimatedLine } from '@mui/x-charts/LineChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesAnimatedLine = createTypes(import.meta.url, AnimatedLine);
