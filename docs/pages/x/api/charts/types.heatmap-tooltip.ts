import { HeatmapTooltip } from '@mui/x-charts-pro/Heatmap';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesHeatmapTooltip = createTypes(import.meta.url, HeatmapTooltip);
