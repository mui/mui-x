import { FocusedSankeyLink } from '@mui/x-charts-pro/SankeyChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesFocusedSankeyLink = createTypes(import.meta.url, FocusedSankeyLink);
