import { ChartsWrapper } from '@mui/x-charts/ChartsWrapper';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChartsWrapper = createTypes(import.meta.url, ChartsWrapper);
