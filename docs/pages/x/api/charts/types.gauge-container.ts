import { GaugeContainer } from '@mui/x-charts/Gauge';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesGaugeContainer = createTypes(import.meta.url, GaugeContainer);
