import { ChartsContainer } from '@mui/x-charts/ChartsContainer';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChartsContainer = createTypes(import.meta.url, ChartsContainer);
