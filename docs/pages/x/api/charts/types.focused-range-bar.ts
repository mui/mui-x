import { FocusedRangeBar } from '@mui/x-charts-premium/BarChartPremium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesFocusedRangeBar = createTypes(import.meta.url, FocusedRangeBar);
