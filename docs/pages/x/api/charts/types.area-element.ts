import { AreaElement } from '@mui/x-charts/LineChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesAreaElement = createTypes(import.meta.url, AreaElement);
