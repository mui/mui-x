import { LineHighlightElement } from '@mui/x-charts/LineChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesLineHighlightElement = createTypes(import.meta.url, LineHighlightElement);
