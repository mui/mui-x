import { SankeyTooltipContent } from '@mui/x-charts-pro/SankeyChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesSankeyTooltipContent = createTypes(import.meta.url, SankeyTooltipContent);
