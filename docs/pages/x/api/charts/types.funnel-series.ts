import { FunnelSeries } from '@mui/x-charts-premium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesFunnelSeries = createTypes(import.meta.url, FunnelSeries);
