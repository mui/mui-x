import { RadarSeriesArea } from '@mui/x-charts/RadarChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesRadarSeriesArea = createTypes(import.meta.url, RadarSeriesArea);
