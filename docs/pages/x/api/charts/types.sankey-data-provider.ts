import { SankeyDataProvider } from '@mui/x-charts-pro/SankeyChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesSankeyDataProvider = createTypes(import.meta.url, SankeyDataProvider);
