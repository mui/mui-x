import { LegendItemParams } from '@mui/x-charts-premium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesLegendItemParams = createTypes(import.meta.url, LegendItemParams);
