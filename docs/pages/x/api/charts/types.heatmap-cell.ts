import { HeatmapCell } from '@mui/x-charts-pro/Heatmap';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesHeatmapCell = createTypes(import.meta.url, HeatmapCell);
