import { ToolbarButton } from '@mui/x-charts/Toolbar';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesToolbarButton = createTypes(import.meta.url, ToolbarButton);
