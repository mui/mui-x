import { HeatmapSeries } from '@mui/x-charts-premium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesHeatmapSeries = createTypes(import.meta.url, HeatmapSeries);
