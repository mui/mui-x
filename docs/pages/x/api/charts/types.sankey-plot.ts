import { SankeyPlot } from '@mui/x-charts-pro/SankeyChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesSankeyPlot = createTypes(import.meta.url, SankeyPlot);
