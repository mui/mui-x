import { FocusedHeatmapCell } from '@mui/x-charts-pro/Heatmap';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesFocusedHeatmapCell = createTypes(import.meta.url, FocusedHeatmapCell);
