import { RadarMetricLabels } from '@mui/x-charts/RadarChart';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesRadarMetricLabels = createTypes(import.meta.url, RadarMetricLabels);
