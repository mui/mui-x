import { HeatmapTooltipContent } from '@mui/x-charts-pro/Heatmap';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesHeatmapTooltipContent = createTypes(import.meta.url, HeatmapTooltipContent);
