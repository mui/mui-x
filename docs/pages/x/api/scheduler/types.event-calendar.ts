import { EventCalendar } from '@mui/x-scheduler/event-calendar';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesEventCalendar = createTypes(import.meta.url, EventCalendar);
