import { StandaloneEvent } from '@mui/x-scheduler/standalone-event';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesStandaloneEvent = createTypes(import.meta.url, StandaloneEvent);
