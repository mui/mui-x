import { StandaloneWeekView } from '@mui/x-scheduler/week-view';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesStandaloneWeekView = createTypes(import.meta.url, StandaloneWeekView);
