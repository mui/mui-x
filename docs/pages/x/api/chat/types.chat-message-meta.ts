import { ChatMessageMeta } from '@mui/x-chat/ChatMessage';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatMessageMeta = createTypes(import.meta.url, ChatMessageMeta);
