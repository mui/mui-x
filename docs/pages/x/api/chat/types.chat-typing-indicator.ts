import { ChatTypingIndicator } from '@mui/x-chat/ChatIndicators';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatTypingIndicator = createTypes(import.meta.url, ChatTypingIndicator);
