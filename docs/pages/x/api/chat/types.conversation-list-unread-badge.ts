import { ConversationListUnreadBadge } from '@mui/x-chat/headless';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesConversationListUnreadBadge = createTypes(import.meta.url, ConversationListUnreadBadge);
