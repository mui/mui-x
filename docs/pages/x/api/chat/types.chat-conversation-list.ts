import { ChatConversationList } from '@mui/x-chat/ChatConversationList';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatConversationList = createTypes(import.meta.url, ChatConversationList);
