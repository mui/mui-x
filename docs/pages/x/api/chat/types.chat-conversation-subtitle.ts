import { ChatConversationSubtitle } from '@mui/x-chat/ChatConversation';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatConversationSubtitle = createTypes(import.meta.url, ChatConversationSubtitle);
