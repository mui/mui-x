# Chat

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/chat/types.chat-message-skeleton.md'

## API Reference

### ChatMessageSkeleton

**ChatMessageSkeleton Props:**

| Prop      | Type                                  | Default | Description                        |
| :-------- | :------------------------------------ | :------ | :--------------------------------- |
| classes   | `Partial<ChatMessageSkeletonClasses>` | -       | -                                  |
| lines     | `number`                              | `3`     | Number of shimmer lines to render. |
| slotProps | `ChatMessageSkeletonSlotProps`        | -       | -                                  |
| slots     | `ChatMessageSkeletonSlots`            | -       | -                                  |
| className | `string`                              | -       | -                                  |

### getChatMessageSkeletonUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

## Additional Types

### chatMessageSkeletonClasses

```typescript
type chatMessageSkeletonClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to each shimmer line element. */
  line: string;
};
```

### ChatMessageSkeletonClasses

```typescript
type ChatMessageSkeletonClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to each shimmer line element. */
  line: string;
};
```

### ChatMessageSkeletonClassKey

```typescript
type ChatMessageSkeletonClassKey = 'root' | 'line';
```

### ChatMessageSkeletonProps

```typescript
type ChatMessageSkeletonProps = {
  /**
   * Number of shimmer lines to render.
   * @default 3
   */
  lines?: number;
  className?: string;
  classes?: Partial<ChatMessageSkeletonClasses>;
  slots?: ChatMessageSkeletonSlots;
  slotProps?: ChatMessageSkeletonSlotProps;
};
```

### ChatMessageSkeletonSlotProps

```typescript
type ChatMessageSkeletonSlotProps = {
  root?: SlotComponentProps<'div', {}, {}>;
  line?: SlotComponentProps<'div', {}, {}>;
};
```

### ChatMessageSkeletonSlots

```typescript
type ChatMessageSkeletonSlots = {
  /**
   * The root container element.
   * @default 'div'
   */
  root?: React.ElementType;
  /**
   * Each animated shimmer line element.
   * @default 'div'
   */
  line?: React.ElementType;
};
```
