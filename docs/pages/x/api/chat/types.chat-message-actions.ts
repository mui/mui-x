import { ChatMessageActions } from '@mui/x-chat/ChatMessage';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatMessageActions = createTypes(import.meta.url, ChatMessageActions);
