# Chat

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/chat/types.chat-message-source.md'

## API Reference

### ChatMessageSource

**ChatMessageSource Props:**

| Prop      | Type                                | Default | Description                                                      |
| :-------- | :---------------------------------- | :------ | :--------------------------------------------------------------- |
| classes   | `Partial<ChatMessageSourceClasses>` | -       | -                                                                |
| href\*    | `string`                            | -       | The URL this source links to.                                    |
| index     | `number`                            | -       | 1-based position number displayed as a muted badge.              |
| slotProps | `ChatMessageSourceSlotProps`        | -       | -                                                                |
| slots     | `ChatMessageSourceSlots`            | -       | -                                                                |
| title     | `string`                            | -       | Display title for the source. Falls back to `href` when omitted. |
| children  | `React.ReactNode`                   | -       | -                                                                |
| className | `string`                            | -       | -                                                                |

### ChatMessageSources

**ChatMessageSources Props:**

| Prop      | Type                                 | Default     | Description                                |
| :-------- | :----------------------------------- | :---------- | :----------------------------------------- |
| label     | `string`                             | `'Sources'` | Label displayed above the list of sources. |
| classes   | `Partial<ChatMessageSourcesClasses>` | -           | -                                          |
| slotProps | `ChatMessageSourcesSlotProps`        | -           | -                                          |
| slots     | `ChatMessageSourcesSlots`            | -           | -                                          |
| children  | `React.ReactNode`                    | -           | -                                          |
| className | `string`                             | -           | -                                          |

### getChatMessageSourcesUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### getChatMessageSourceUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

## Additional Types

### chatMessageSourceClasses

```typescript
type chatMessageSourceClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the index badge element. */
  index: string;
  /** Styles applied to the link element. */
  link: string;
};
```

### ChatMessageSourceClasses

```typescript
type ChatMessageSourceClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the index badge element. */
  index: string;
  /** Styles applied to the link element. */
  link: string;
};
```

### ChatMessageSourceClassKey

```typescript
type ChatMessageSourceClassKey = 'root' | 'link' | 'index';
```

### ChatMessageSourceProps

```typescript
type ChatMessageSourceProps = {
  /** The URL this source links to. */
  href: string;
  /** Display title for the source. Falls back to `href` when omitted. */
  title?: string;
  /** 1-based position number displayed as a muted badge. */
  index?: number;
  children?: React.ReactNode;
  className?: string;
  classes?: Partial<ChatMessageSourceClasses>;
  slots?: ChatMessageSourceSlots;
  slotProps?: ChatMessageSourceSlotProps;
};
```

### chatMessageSourcesClasses

```typescript
type chatMessageSourcesClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the label element. */
  label: string;
  /** Styles applied to the list element. */
  list: string;
};
```

### ChatMessageSourcesClasses

```typescript
type ChatMessageSourcesClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the label element. */
  label: string;
  /** Styles applied to the list element. */
  list: string;
};
```

### ChatMessageSourcesClassKey

```typescript
type ChatMessageSourcesClassKey = 'root' | 'label' | 'list';
```

### ChatMessageSourceSlotProps

```typescript
type ChatMessageSourceSlotProps = {
  root?: SlotComponentProps<'li', {}, {}>;
  index?: SlotComponentProps<'span', {}, {}>;
  link?: SlotComponentProps<'a', {}, {}>;
};
```

### ChatMessageSourceSlots

```typescript
type ChatMessageSourceSlots = {
  /**
   * The root list item element.
   * @default 'li'
   */
  root?: React.ElementType;
  /**
   * The numeric index badge.
   * @default 'span'
   */
  index?: React.ElementType;
  /**
   * The anchor link.
   * @default 'a'
   */
  link?: React.ElementType;
};
```

### ChatMessageSourcesProps

```typescript
type ChatMessageSourcesProps = {
  /**
   * Label displayed above the list of sources.
   * @default 'Sources'
   */
  label?: string;
  children?: React.ReactNode;
  className?: string;
  classes?: Partial<ChatMessageSourcesClasses>;
  slots?: ChatMessageSourcesSlots;
  slotProps?: ChatMessageSourcesSlotProps;
};
```

### ChatMessageSourcesSlotProps

```typescript
type ChatMessageSourcesSlotProps = {
  root?: SlotComponentProps<'div', {}, {}>;
  label?: SlotComponentProps<'p', {}, {}>;
  list?: SlotComponentProps<'ol', {}, {}>;
};
```

### ChatMessageSourcesSlots

```typescript
type ChatMessageSourcesSlots = {
  /**
   * The root container element.
   * @default 'div'
   */
  root?: React.ElementType;
  /**
   * The label element shown above the list.
   * @default 'p'
   */
  label?: React.ElementType;
  /**
   * The ordered list wrapping source items.
   * @default 'ol'
   */
  list?: React.ElementType;
};
```
