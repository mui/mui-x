import { ChatMessageInlineMeta } from '@mui/x-chat/ChatMessage';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatMessageInlineMeta = createTypes(import.meta.url, ChatMessageInlineMeta);
