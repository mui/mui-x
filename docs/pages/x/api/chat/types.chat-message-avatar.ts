import { ChatMessageAvatar } from '@mui/x-chat/ChatMessage';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatMessageAvatar = createTypes(import.meta.url, ChatMessageAvatar);
