import { ChatMessageSources } from '@mui/x-chat/ChatMessageSources';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatMessageSources = createTypes(import.meta.url, ChatMessageSources);
