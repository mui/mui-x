import { ConversationTitle } from '@mui/x-chat/headless';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesConversationTitle = createTypes(import.meta.url, ConversationTitle);
