import { ChatConversationHeaderInfo } from '@mui/x-chat/ChatConversation';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatConversationHeaderInfo = createTypes(import.meta.url, ChatConversationHeaderInfo);
