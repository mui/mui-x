import { MessageAvatar } from '@mui/x-chat/headless';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesMessageAvatar = createTypes(import.meta.url, MessageAvatar);
