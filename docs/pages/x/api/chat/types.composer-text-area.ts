import { ComposerTextArea } from '@mui/x-chat/headless';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesComposerTextArea = createTypes(import.meta.url, ComposerTextArea);
