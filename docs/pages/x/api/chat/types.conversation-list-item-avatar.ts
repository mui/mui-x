import { ConversationListItemAvatar } from '@mui/x-chat/headless';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesConversationListItemAvatar = createTypes(import.meta.url, ConversationListItemAvatar);
