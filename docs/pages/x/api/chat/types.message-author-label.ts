import { MessageAuthorLabel } from '@mui/x-chat/headless';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesMessageAuthorLabel = createTypes(import.meta.url, MessageAuthorLabel);
