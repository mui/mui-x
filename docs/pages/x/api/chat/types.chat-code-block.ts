import { ChatCodeBlock } from '@mui/x-chat/ChatCodeBlock';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatCodeBlock = createTypes(import.meta.url, ChatCodeBlock);
