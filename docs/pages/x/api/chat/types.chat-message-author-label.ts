import { ChatMessageAuthorLabel } from '@mui/x-chat/ChatMessage';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatMessageAuthorLabel = createTypes(import.meta.url, ChatMessageAuthorLabel);
