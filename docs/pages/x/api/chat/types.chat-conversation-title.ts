import { ChatConversationTitle } from '@mui/x-chat/ChatConversation';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatConversationTitle = createTypes(import.meta.url, ChatConversationTitle);
