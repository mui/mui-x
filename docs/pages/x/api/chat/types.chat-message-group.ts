import { ChatMessageGroup } from '@mui/x-chat/ChatMessage';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatMessageGroup = createTypes(import.meta.url, ChatMessageGroup);
