import { ComposerAttachmentList } from '@mui/x-chat/headless';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesComposerAttachmentList = createTypes(import.meta.url, ComposerAttachmentList);
