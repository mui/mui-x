import { ChatConfirmation } from '@mui/x-chat/ChatConfirmation';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatConfirmation = createTypes(import.meta.url, ChatConfirmation);
