import { ChatMessageList } from '@mui/x-chat/ChatMessageList';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesChatMessageList = createTypes(import.meta.url, ChatMessageList);
