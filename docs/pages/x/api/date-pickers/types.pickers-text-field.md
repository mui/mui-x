# Date Pickers

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/date-pickers/types.pickers-text-field.md'

## API Reference

### getPickersFilledInputUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### getPickersInputBaseUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### getPickersInputUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### getPickersOutlinedInputUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### getPickersTextFieldUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### PickersTextField

**PickersTextField Props:**

| Prop                  | Type                                                                                                                                                         | Default      | Description                                                                                                                                                                       |
| :-------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------- | :----------- | :-------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| name                  | `string`                                                                                                                                                     | -            | Name attribute of the `input` element.                                                                                                                                            |
| label                 | `React.ReactNode`                                                                                                                                            | -            | The label content.                                                                                                                                                                |
| value\*               | `string`                                                                                                                                                     | -            | -                                                                                                                                                                                 |
| onClick\*             | `React.MouseEventHandler<HTMLDivElement>`                                                                                                                    | -            | -                                                                                                                                                                                 |
| areAllSectionsEmpty\* | `boolean`                                                                                                                                                    | -            | Is `true` if the current values equals the empty value.&#xA;For a single item value, it means that `value === null`&#xA;For a range value, it means that `value === [null, null]` |
| contentEditable\*     | `boolean`                                                                                                                                                    | -            | If true, the whole element is editable.&#xA;Useful when all the sections are selected.                                                                                            |
| elements\*            | `PickersSectionElement[]`                                                                                                                                    | -            | The elements to render.&#xA;Each element contains the prop to edit a section of the value.                                                                                        |
| endAdornment          | `React.ReactNode`                                                                                                                                            | -            | End `InputAdornment` for this component.                                                                                                                                          |
| error\*               | `boolean`                                                                                                                                                    | `false`      | If `true`, the `input` will indicate an error.                                                                                                                                    |
| fullWidth             | `boolean`                                                                                                                                                    | `false`      | If `true`, the input will take up the full width of its container.                                                                                                                |
| helperText            | `React.ReactNode`                                                                                                                                            | -            | The helper text content.                                                                                                                                                          |
| onBlur\*              | `React.FocusEventHandler<HTMLDivElement>`                                                                                                                    | -            | -                                                                                                                                                                                 |
| onChange\*            | `React.ChangeEventHandler<HTMLInputElement, Element>`                                                                                                        | -            | -                                                                                                                                                                                 |
| onFocus\*             | `React.FocusEventHandler<HTMLDivElement>`                                                                                                                    | -            | -                                                                                                                                                                                 |
| onInput\*             | `React.FormEventHandler<HTMLDivElement>`                                                                                                                     | -            | -                                                                                                                                                                                 |
| onKeyDown\*           | `React.EventHandler`                                                                                                                                         | -            | -                                                                                                                                                                                 |
| onPaste\*             | `React.ClipboardEventHandler<HTMLDivElement>`                                                                                                                | -            | -                                                                                                                                                                                 |
| sectionListRef\*      | `React.Ref<PickersSectionListRef>`                                                                                                                           | -            | -                                                                                                                                                                                 |
| slotProps             | `PickersTextFieldSlotProps<PickersFilledInputProps> \| PickersTextFieldSlotProps<PickersOutlinedInputProps> \| PickersTextFieldSlotProps<PickersInputProps>` | `{}`         | The props used for each component slot.                                                                                                                                           |
| slots                 | `PickersTextFieldSlots`                                                                                                                                      | `{}`         | The components used for each slot inside.                                                                                                                                         |
| startAdornment        | `React.ReactNode`                                                                                                                                            | -            | Start `InputAdornment` for this component.                                                                                                                                        |
| variant               | `'filled' \| 'outlined' \| 'standard'`                                                                                                                       | `'outlined'` | The variant to use.                                                                                                                                                               |
| disabled\*            | `boolean`                                                                                                                                                    | -            | -                                                                                                                                                                                 |
| readOnly              | `boolean`                                                                                                                                                    | -            | -                                                                                                                                                                                 |
| inputRef              | `React.Ref<HTMLInputElement>`                                                                                                                                | -            | Pass a ref to the `input` element.                                                                                                                                                |
| id                    | `string`                                                                                                                                                     | -            | The id of the `input` element.                                                                                                                                                    |

## Additional Types

### PickersFilledInput

```typescript
type PickersFilledInput = unknown;
```

### pickersFilledInputClasses

```typescript
type pickersFilledInputClasses = {};
```

### PickersFilledInputClasses

```typescript
type PickersFilledInputClasses = {
  /** Styles applied to the root element unless `disableUnderline={true}`. */
  underline: string;
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the root element if focused. */
  focused: string;
  /** State class applied to the root element if `disabled=true`. */
  disabled: string;
  /** State class applied to the root element if `readOnly=true`. */
  readOnly: string;
  /** State class applied to the root element if `error=true`. */
  error: string;
  /** State class applied to the root element if `size=small`. */
  inputSizeSmall: string;
  /** Styles applied to the NotchedOutline element. */
  notchedOutline: string;
  /** Styles applied to the real hidden input element. */
  input: string;
  /** Styles applied to the container of the sections. */
  sectionsContainer: string;
  /** Styles applied to the content of a section. */
  sectionContent: string;
  /** Styles applied to the separator before a section. */
  sectionBefore: string;
  /** Styles applied to the separator after a section. */
  sectionAfter: string;
  /** Styles applied to the root if there is a startAdornment present. */
  adornedStart: string;
  /** Styles applied to the root if there is an endAdornment present. */
  adornedEnd: string;
  /** Styles applied to the active section bar. */
  activeBar: string;
};
```

### PickersFilledInputClassKey

```typescript
type PickersFilledInputClassKey =
  | 'disabled'
  | 'error'
  | 'focused'
  | 'input'
  | 'readOnly'
  | 'root'
  | 'underline'
  | 'inputSizeSmall'
  | 'notchedOutline'
  | 'sectionsContainer'
  | 'sectionContent'
  | 'sectionBefore'
  | 'sectionAfter'
  | 'adornedStart'
  | 'adornedEnd'
  | 'activeBar';
```

### PickersFilledInputProps

```typescript
type PickersFilledInputProps = {
  disableUnderline?: boolean;
  hiddenLabel?: boolean;
  ownerState?: PickerTextFieldOwnerState;
  className?: string;
  classes?: Record<string, string>;
  /** The system prop that allows defining system overrides as well as additional CSS styles. */
  sx?: SxProps<Theme>;
  margin?: 'dense' | 'none' | 'normal';
  renderSuffix?: (state: {
    disabled?: boolean;
    error?: boolean;
    filled?: boolean;
    focused?: boolean;
    margin?: 'dense' | 'none' | 'normal';
    required?: boolean;
    adornedStart?: boolean;
  }) => React.ReactNode;
  ref?: React.Ref<HTMLDivElement>;
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots?: PickersInputBaseSlots;
  /**
   * The props used for each component slot.
   * @default {}
   */
  slotProps?: PickersInputBaseSlotProps;
  'data-multi-input'?: string;
  /**
   * Is `true` if the current values equals the empty value.
   * For a single item value, it means that `value === null`
   * For a range value, it means that `value === [null, null]`
   */
  areAllSectionsEmpty: boolean;
  onClick: React.MouseEventHandler<HTMLDivElement>;
  onKeyDown: React.EventHandler;
  onInput: React.FormEventHandler<HTMLDivElement>;
  onPaste: React.ClipboardEventHandler<HTMLDivElement>;
  /** End `InputAdornment` for this component. */
  endAdornment?: React.ReactNode;
  /** Start `InputAdornment` for this component. */
  startAdornment?: React.ReactNode;
  value: string;
  onChange: React.ChangeEventHandler<HTMLInputElement, Element>;
  /** The label content. */
  label?: React.ReactNode;
  /** The id of the `input` element. */
  id?: string;
  /**
   * If `true`, the input will take up the full width of its container.
   * @default false
   */
  fullWidth?: boolean;
  readOnly?: boolean;
  /** Name attribute of the `input` element. */
  name?: string;
  /** Pass a ref to the `input` element. */
  inputRef?: React.Ref<HTMLInputElement>;
  /**
   * The elements to render.
   * Each element contains the prop to edit a section of the value.
   */
  elements: PickersSectionElement[];
  sectionListRef: React.Ref<PickersSectionListRef>;
  /**
   * If true, the whole element is editable.
   * Useful when all the sections are selected.
   */
  contentEditable: boolean;
};
```

### PickersInput

```typescript
type PickersInput = unknown;
```

### PickersInputBase

```typescript
type PickersInputBase = unknown;
```

### pickersInputBaseClasses

```typescript
type pickersInputBaseClasses = Record<
  | 'disabled'
  | 'error'
  | 'focused'
  | 'input'
  | 'readOnly'
  | 'root'
  | 'inputSizeSmall'
  | 'notchedOutline'
  | 'sectionsContainer'
  | 'sectionContent'
  | 'sectionBefore'
  | 'sectionAfter'
  | 'adornedStart'
  | 'adornedEnd'
  | 'activeBar',
  string
>;
```

### PickersInputBaseClasses

```typescript
type PickersInputBaseClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the root element if focused. */
  focused: string;
  /** State class applied to the root element if `disabled=true`. */
  disabled: string;
  /** State class applied to the root element if `readOnly=true`. */
  readOnly: string;
  /** State class applied to the root element if `error=true`. */
  error: string;
  /** State class applied to the root element if `size=small`. */
  inputSizeSmall: string;
  /** Styles applied to the NotchedOutline element. */
  notchedOutline: string;
  /** Styles applied to the real hidden input element. */
  input: string;
  /** Styles applied to the container of the sections. */
  sectionsContainer: string;
  /** Styles applied to the content of a section. */
  sectionContent: string;
  /** Styles applied to the separator before a section. */
  sectionBefore: string;
  /** Styles applied to the separator after a section. */
  sectionAfter: string;
  /** Styles applied to the root if there is a startAdornment present. */
  adornedStart: string;
  /** Styles applied to the root if there is an endAdornment present. */
  adornedEnd: string;
  /** Styles applied to the active section bar. */
  activeBar: string;
};
```

### PickersInputBaseClassKey

```typescript
type PickersInputBaseClassKey =
  | 'disabled'
  | 'error'
  | 'focused'
  | 'input'
  | 'readOnly'
  | 'root'
  | 'inputSizeSmall'
  | 'notchedOutline'
  | 'sectionsContainer'
  | 'sectionContent'
  | 'sectionBefore'
  | 'sectionAfter'
  | 'adornedStart'
  | 'adornedEnd'
  | 'activeBar';
```

### PickersInputBaseProps

```typescript
type PickersInputBaseProps = {
  ownerState?: PickerTextFieldOwnerState;
  className?: string;
  classes?: Record<string, string>;
  /** The system prop that allows defining system overrides as well as additional CSS styles. */
  sx?: SxProps<Theme>;
  margin?: 'dense' | 'none' | 'normal';
  renderSuffix?: (state: {
    disabled?: boolean;
    error?: boolean;
    filled?: boolean;
    focused?: boolean;
    margin?: 'dense' | 'none' | 'normal';
    required?: boolean;
    adornedStart?: boolean;
  }) => React.ReactNode;
  ref?: React.Ref<HTMLDivElement>;
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots?: PickersInputBaseSlots;
  /**
   * The props used for each component slot.
   * @default {}
   */
  slotProps?: PickersInputBaseSlotProps;
  'data-multi-input'?: string;
  /**
   * Is `true` if the current values equals the empty value.
   * For a single item value, it means that `value === null`
   * For a range value, it means that `value === [null, null]`
   */
  areAllSectionsEmpty: boolean;
  onClick: React.MouseEventHandler<HTMLDivElement>;
  onKeyDown: React.EventHandler;
  onInput: React.FormEventHandler<HTMLDivElement>;
  onPaste: React.ClipboardEventHandler<HTMLDivElement>;
  /** End `InputAdornment` for this component. */
  endAdornment?: React.ReactNode;
  /** Start `InputAdornment` for this component. */
  startAdornment?: React.ReactNode;
  value: string;
  onChange: React.ChangeEventHandler<HTMLInputElement, Element>;
  /** The label content. */
  label?: React.ReactNode;
  /** The id of the `input` element. */
  id?: string;
  /**
   * If `true`, the input will take up the full width of its container.
   * @default false
   */
  fullWidth?: boolean;
  readOnly?: boolean;
  /** Name attribute of the `input` element. */
  name?: string;
  /** Pass a ref to the `input` element. */
  inputRef?: React.Ref<HTMLInputElement>;
  /**
   * The elements to render.
   * Each element contains the prop to edit a section of the value.
   */
  elements: PickersSectionElement[];
  sectionListRef: React.Ref<PickersSectionListRef>;
  /**
   * If true, the whole element is editable.
   * Useful when all the sections are selected.
   */
  contentEditable: boolean;
};
```

### pickersInputClasses

```typescript
type pickersInputClasses = {};
```

### PickersInputClasses

```typescript
type PickersInputClasses = {
  /** Styles applied to the root element unless `disableUnderline={true}`. */
  underline: string;
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the root element if focused. */
  focused: string;
  /** State class applied to the root element if `disabled=true`. */
  disabled: string;
  /** State class applied to the root element if `readOnly=true`. */
  readOnly: string;
  /** State class applied to the root element if `error=true`. */
  error: string;
  /** State class applied to the root element if `size=small`. */
  inputSizeSmall: string;
  /** Styles applied to the NotchedOutline element. */
  notchedOutline: string;
  /** Styles applied to the real hidden input element. */
  input: string;
  /** Styles applied to the container of the sections. */
  sectionsContainer: string;
  /** Styles applied to the content of a section. */
  sectionContent: string;
  /** Styles applied to the separator before a section. */
  sectionBefore: string;
  /** Styles applied to the separator after a section. */
  sectionAfter: string;
  /** Styles applied to the root if there is a startAdornment present. */
  adornedStart: string;
  /** Styles applied to the root if there is an endAdornment present. */
  adornedEnd: string;
  /** Styles applied to the active section bar. */
  activeBar: string;
};
```

### PickersInputClassKey

```typescript
type PickersInputClassKey =
  | 'disabled'
  | 'error'
  | 'focused'
  | 'input'
  | 'readOnly'
  | 'root'
  | 'underline'
  | 'inputSizeSmall'
  | 'notchedOutline'
  | 'sectionsContainer'
  | 'sectionContent'
  | 'sectionBefore'
  | 'sectionAfter'
  | 'adornedStart'
  | 'adornedEnd'
  | 'activeBar';
```

### PickersInputProps

```typescript
type PickersInputProps = {
  disableUnderline?: boolean;
  ownerState?: PickerTextFieldOwnerState;
  className?: string;
  classes?: Record<string, string>;
  /** The system prop that allows defining system overrides as well as additional CSS styles. */
  sx?: SxProps<Theme>;
  margin?: 'dense' | 'none' | 'normal';
  renderSuffix?: (state: {
    disabled?: boolean;
    error?: boolean;
    filled?: boolean;
    focused?: boolean;
    margin?: 'dense' | 'none' | 'normal';
    required?: boolean;
    adornedStart?: boolean;
  }) => React.ReactNode;
  ref?: React.Ref<HTMLDivElement>;
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots?: PickersInputBaseSlots;
  /**
   * The props used for each component slot.
   * @default {}
   */
  slotProps?: PickersInputBaseSlotProps;
  'data-multi-input'?: string;
  /**
   * Is `true` if the current values equals the empty value.
   * For a single item value, it means that `value === null`
   * For a range value, it means that `value === [null, null]`
   */
  areAllSectionsEmpty: boolean;
  onClick: React.MouseEventHandler<HTMLDivElement>;
  onKeyDown: React.EventHandler;
  onInput: React.FormEventHandler<HTMLDivElement>;
  onPaste: React.ClipboardEventHandler<HTMLDivElement>;
  /** End `InputAdornment` for this component. */
  endAdornment?: React.ReactNode;
  /** Start `InputAdornment` for this component. */
  startAdornment?: React.ReactNode;
  value: string;
  onChange: React.ChangeEventHandler<HTMLInputElement, Element>;
  /** The label content. */
  label?: React.ReactNode;
  /** The id of the `input` element. */
  id?: string;
  /**
   * If `true`, the input will take up the full width of its container.
   * @default false
   */
  fullWidth?: boolean;
  readOnly?: boolean;
  /** Name attribute of the `input` element. */
  name?: string;
  /** Pass a ref to the `input` element. */
  inputRef?: React.Ref<HTMLInputElement>;
  /**
   * The elements to render.
   * Each element contains the prop to edit a section of the value.
   */
  elements: PickersSectionElement[];
  sectionListRef: React.Ref<PickersSectionListRef>;
  /**
   * If true, the whole element is editable.
   * Useful when all the sections are selected.
   */
  contentEditable: boolean;
};
```

### PickersOutlinedInput

```typescript
type PickersOutlinedInput = unknown;
```

### pickersOutlinedInputClasses

```typescript
type pickersOutlinedInputClasses = {};
```

### PickersOutlinedInputClasses

```typescript
type PickersOutlinedInputClasses = {
  /** Styles applied to the NotchedOutline element. */
  notchedOutline: string;
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the root element if focused. */
  focused: string;
  /** State class applied to the root element if `disabled=true`. */
  disabled: string;
  /** State class applied to the root element if `readOnly=true`. */
  readOnly: string;
  /** State class applied to the root element if `error=true`. */
  error: string;
  /** State class applied to the root element if `size=small`. */
  inputSizeSmall: string;
  /** Styles applied to the real hidden input element. */
  input: string;
  /** Styles applied to the container of the sections. */
  sectionsContainer: string;
  /** Styles applied to the content of a section. */
  sectionContent: string;
  /** Styles applied to the separator before a section. */
  sectionBefore: string;
  /** Styles applied to the separator after a section. */
  sectionAfter: string;
  /** Styles applied to the root if there is a startAdornment present. */
  adornedStart: string;
  /** Styles applied to the root if there is an endAdornment present. */
  adornedEnd: string;
  /** Styles applied to the active section bar. */
  activeBar: string;
};
```

### PickersOutlinedInputClassKey

```typescript
type PickersOutlinedInputClassKey =
  | 'disabled'
  | 'error'
  | 'focused'
  | 'input'
  | 'readOnly'
  | 'root'
  | 'inputSizeSmall'
  | 'notchedOutline'
  | 'sectionsContainer'
  | 'sectionContent'
  | 'sectionBefore'
  | 'sectionAfter'
  | 'adornedStart'
  | 'adornedEnd'
  | 'activeBar';
```

### PickersOutlinedInputProps

```typescript
type PickersOutlinedInputProps = {
  notched?: boolean;
  ownerState?: PickerTextFieldOwnerState;
  className?: string;
  classes?: Record<string, string>;
  /** The system prop that allows defining system overrides as well as additional CSS styles. */
  sx?: SxProps<Theme>;
  margin?: 'dense' | 'none' | 'normal';
  renderSuffix?: (state: {
    disabled?: boolean;
    error?: boolean;
    filled?: boolean;
    focused?: boolean;
    margin?: 'dense' | 'none' | 'normal';
    required?: boolean;
    adornedStart?: boolean;
  }) => React.ReactNode;
  ref?: React.Ref<HTMLDivElement>;
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots?: PickersInputBaseSlots;
  /**
   * The props used for each component slot.
   * @default {}
   */
  slotProps?: PickersInputBaseSlotProps;
  'data-multi-input'?: string;
  /**
   * Is `true` if the current values equals the empty value.
   * For a single item value, it means that `value === null`
   * For a range value, it means that `value === [null, null]`
   */
  areAllSectionsEmpty: boolean;
  onClick: React.MouseEventHandler<HTMLDivElement>;
  onKeyDown: React.EventHandler;
  onInput: React.FormEventHandler<HTMLDivElement>;
  onPaste: React.ClipboardEventHandler<HTMLDivElement>;
  /** End `InputAdornment` for this component. */
  endAdornment?: React.ReactNode;
  /** Start `InputAdornment` for this component. */
  startAdornment?: React.ReactNode;
  value: string;
  onChange: React.ChangeEventHandler<HTMLInputElement, Element>;
  /** The label content. */
  label?: React.ReactNode;
  /** The id of the `input` element. */
  id?: string;
  /**
   * If `true`, the input will take up the full width of its container.
   * @default false
   */
  fullWidth?: boolean;
  readOnly?: boolean;
  /** Name attribute of the `input` element. */
  name?: string;
  /** Pass a ref to the `input` element. */
  inputRef?: React.Ref<HTMLInputElement>;
  /**
   * The elements to render.
   * Each element contains the prop to edit a section of the value.
   */
  elements: PickersSectionElement[];
  sectionListRef: React.Ref<PickersSectionListRef>;
  /**
   * If true, the whole element is editable.
   * Useful when all the sections are selected.
   */
  contentEditable: boolean;
};
```

### pickersTextFieldClasses

```typescript
type pickersTextFieldClasses = Record<
  'disabled' | 'error' | 'focused' | 'required' | 'root',
  string
>;
```

### PickersTextFieldClasses

```typescript
type PickersTextFieldClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the root element if focused. */
  focused: string;
  /** State class applied to the root element if `disabled=true`. */
  disabled: string;
  /** State class applied to the root element if `error=true`. */
  error: string;
  /** State class applied to the root element id `required=true` */
  required: string;
};
```

### PickersTextFieldClassKey

```typescript
type PickersTextFieldClassKey = 'disabled' | 'error' | 'focused' | 'required' | 'root';
```

### PickersTextFieldProps

```typescript
type PickersTextFieldProps<Variant extends TextFieldVariants = TextFieldVariants> = {
  /**
   * The variant to use.
   * @default 'outlined'
   */
  variant?: 'filled' | 'outlined' | 'standard';
  /** The helper text content. */
  helperText?: React.ReactNode;
  /**
   * Is `true` if the current values equals the empty value.
   * For a single item value, it means that `value === null`
   * For a range value, it means that `value === [null, null]`
   */
  areAllSectionsEmpty: boolean;
  onClick: React.MouseEventHandler<HTMLDivElement>;
  onKeyDown: React.EventHandler;
  onInput: React.FormEventHandler<HTMLDivElement>;
  onPaste: React.ClipboardEventHandler<HTMLDivElement>;
  /** End `InputAdornment` for this component. */
  endAdornment?: React.ReactNode;
  /** Start `InputAdornment` for this component. */
  startAdornment?: React.ReactNode;
  value: string;
  onChange: React.ChangeEventHandler<HTMLInputElement, Element>;
  /** The label content. */
  label?: React.ReactNode;
  /** The id of the `input` element. */
  id?: string;
  /**
   * If `true`, the input will take up the full width of its container.
   * @default false
   */
  fullWidth?: boolean;
  readOnly?: boolean;
  /** Name attribute of the `input` element. */
  name?: string;
  /** Pass a ref to the `input` element. */
  inputRef?: React.Ref<HTMLInputElement>;
  /**
   * The elements to render.
   * Each element contains the prop to edit a section of the value.
   */
  elements: PickersSectionElement[];
  sectionListRef: React.Ref<PickersSectionListRef>;
  /**
   * If true, the whole element is editable.
   * Useful when all the sections are selected.
   */
  contentEditable: boolean;
  onFocus: React.FocusEventHandler<HTMLDivElement>;
  onBlur: React.FocusEventHandler<HTMLDivElement>;
  disabled: boolean;
  /**
   * If `true`, the `input` will indicate an error.
   * @default false
   */
  error: boolean;
  /**
   * The components used for each slot inside.
   * @default {}
   */
  slots?: PickersTextFieldSlots;
  /**
   * The props used for each component slot.
   * @default {}
   */
  slotProps?:
    | PickersTextFieldSlotProps<PickersFilledInputProps>
    | PickersTextFieldSlotProps<PickersOutlinedInputProps>
    | PickersTextFieldSlotProps<PickersInputProps>;
};
```

### PickersTextFieldSlotProps

```typescript
type PickersTextFieldSlotProps<InputPropsType extends PickersInputBaseProps> = {
  root?: Partial<FormControlProps>;
  input?: Partial<InputPropsType>;
  inputLabel?: Partial<InputLabelProps>;
  htmlInput?: React.ComponentPropsWithRef<'input'>;
  formHelperText?: Partial<FormHelperTextProps>;
};
```

### PickersTextFieldSlots

```typescript
type PickersTextFieldSlots = {
  /**
   * The component used for the root slot.
   * @default FormControl
   */
  root?: React.ElementType;
  /**
   * The component used for the input slot.
   * Defaults to one of `PickersInput`, `PickersFilledInput`, `PickersOutlinedInput` based on `variant`.
   * @default PickersOutlinedInput
   */
  input?: React.ElementType;
  /**
   * The component used for the input label slot.
   * @default InputLabel
   */
  inputLabel?: React.ElementType;
  /**
   * The component rendered as the underlying hidden `<input>` element.
   * @default PickersInputBaseInput
   */
  htmlInput?: React.ElementType;
  /**
   * The component used for the form helper text slot.
   * @default FormHelperText
   */
  formHelperText?: React.ElementType;
};
```
