import { PickerDay } from '@mui/x-date-pickers/PickerDay';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesPickerDay = createTypes(import.meta.url, PickerDay);
