import { DateCalendar } from '@mui/x-date-pickers/DateCalendar';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesDateCalendar = createTypes(import.meta.url, DateCalendar);
