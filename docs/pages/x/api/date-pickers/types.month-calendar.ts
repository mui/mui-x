import { MonthCalendar } from '@mui/x-date-pickers/MonthCalendar';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesMonthCalendar = createTypes(import.meta.url, MonthCalendar);
