import { TimeField } from '@mui/x-date-pickers/TimeField';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesTimeField = createTypes(import.meta.url, TimeField);
