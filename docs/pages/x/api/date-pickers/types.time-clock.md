# Date Pickers

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/date-pickers/types.time-clock.md'

## API Reference

### getTimeClockUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### TimeClock

Demos:

- [TimePicker](https://mui.com/x/react-date-pickers/time-picker/)
- [TimeClock](https://mui.com/x/react-date-pickers/time-clock/)

API:

- [TimeClock API](https://mui.com/x/api/date-pickers/time-clock/)

**TimeClock Props:**

| Prop                                     | Type                                                                                     | Default                                                                                              | Description                                                                                                                                                                                                                        |
| :--------------------------------------- | :--------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------- | :--------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| defaultValue                             | `any \| null`                                                                            | -                                                                                                    | The default selected value.&#xA;Used when the component is not controlled.                                                                                                                                                         |
| value                                    | `any \| null`                                                                            | -                                                                                                    | The selected value.&#xA;Used when the component is controlled.                                                                                                                                                                     |
| ampm                                     | `boolean`                                                                                | `adapter.is12HourCycleInCurrentLocale()`                                                             | 12h/24h view for hour selection clock.                                                                                                                                                                                             |
| ampmInClock                              | `boolean`                                                                                | `false`                                                                                              | Display ampm controls under the clock (instead of in the toolbar).                                                                                                                                                                 |
| classes                                  | `Partial<TimeClockClasses>`                                                              | -                                                                                                    | Override or extend the styles applied to the component.                                                                                                                                                                            |
| disableFuture                            | `boolean`                                                                                | `false`                                                                                              | If `true`, disable values after the current date for date components, time for time components and both for date time components.                                                                                                  |
| disableIgnoringDatePartForTimeValidation | `boolean`                                                                                | `false`                                                                                              | Do not ignore date part when validating min/max time.                                                                                                                                                                              |
| disablePast                              | `boolean`                                                                                | `false`                                                                                              | If `true`, disable values before the current date for date components, time for time components and both for date time components.                                                                                                 |
| focusedView                              | `TimeView \| null`                                                                       | -                                                                                                    | Controlled focused view.                                                                                                                                                                                                           |
| maxTime                                  | `any`                                                                                    | -                                                                                                    | Maximal selectable time.&#xA;The date part of the object will be ignored unless `props.disableIgnoringDatePartForTimeValidation === true`.                                                                                         |
| minTime                                  | `any`                                                                                    | -                                                                                                    | Minimal selectable time.&#xA;The date part of the object will be ignored unless `props.disableIgnoringDatePartForTimeValidation === true`.                                                                                         |
| minutesStep                              | `number`                                                                                 | `1`                                                                                                  | Step over minutes.                                                                                                                                                                                                                 |
| onChange                                 | `((value: any, selectionState?: PickerSelectionState, selectedView?: TimeView) => void)` | -                                                                                                    | Callback fired when the value changes.                                                                                                                                                                                             |
| onFocusedViewChange                      | `((view: TimeView, hasFocus: boolean) => void)`                                          | -                                                                                                    | Callback fired on focused view change.                                                                                                                                                                                             |
| onViewChange                             | `((view: TimeView) => void)`                                                             | -                                                                                                    | Callback fired on view change.                                                                                                                                                                                                     |
| openTo                                   | `TimeView`                                                                               | -                                                                                                    | The default visible view.&#xA;Used when the component view is not controlled.&#xA;Must be a valid option from `views` list.                                                                                                        |
| referenceDate                            | `any`                                                                                    | ``The closest valid time using the validation props, except callbacks such as `shouldDisableTime`.`` | The date used to generate the new value when both `value` and `defaultValue` are empty.                                                                                                                                            |
| shouldDisableTime                        | `((value: any, view: TimeView) => boolean)`                                              | -                                                                                                    | Disable specific time.                                                                                                                                                                                                             |
| showViewSwitcher                         | `boolean`                                                                                | -                                                                                                    | -                                                                                                                                                                                                                                  |
| slotProps                                | `TimeClockSlotProps`                                                                     | `{}`                                                                                                 | The props used for each component slot.                                                                                                                                                                                            |
| slots                                    | `TimeClockSlots`                                                                         | `{}`                                                                                                 | Overridable component slots.                                                                                                                                                                                                       |
| sx                                       | `SxProps<Theme>`                                                                         | -                                                                                                    | The system prop that allows defining system overrides as well as additional CSS styles.                                                                                                                                            |
| timezone                                 | `PickersTimezone`                                                                        | ``The timezone of the `value` or `defaultValue` prop is defined, 'default' otherwise.``              | Choose which timezone to use for the value.&#xA;Example: "default", "system", "UTC", "America/New_York".&#xA;If you pass values from other timezones to some props, they will be converted to this timezone before being used.     |
| view                                     | `TimeView`                                                                               | -                                                                                                    | The visible view.&#xA;Used when the component view is controlled.&#xA;Must be a valid option from `views` list.                                                                                                                    |
| views                                    | `TimeView[]`                                                                             | `['hours', 'minutes']`                                                                               | Available views.                                                                                                                                                                                                                   |
| autoFocus                                | `boolean`                                                                                | -                                                                                                    | If `true`, the main element is focused during the first mount.&#xA;This main element is: the element chosen by the visible view if any (i.e: the selected day on the `day` view).the `input` element if there is a field rendered. |
| disabled                                 | `boolean`                                                                                | `false`                                                                                              | If `true`, the component is disabled.&#xA;When disabled, the value cannot be changed and no interaction is possible.                                                                                                               |
| readOnly                                 | `boolean`                                                                                | `false`                                                                                              | If `true`, the component is read-only.&#xA;When read-only, the value cannot be changed but the user can interact with the interface.                                                                                               |
| className                                | `string`                                                                                 | -                                                                                                    | -                                                                                                                                                                                                                                  |

## Additional Types

### clockClasses

```typescript
type clockClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the clock element. */
  clock: string;
  /** Styles applied to the wrapper element. */
  wrapper: string;
  /** Styles applied to the square mask element. */
  squareMask: string;
  /** Styles applied to the pin element. */
  pin: string;
  /** Styles applied to the am button element. */
  amButton: string;
  /** Styles applied to the pm button element. */
  pmButton: string;
  /** Styles applied to the meridiem typography element. */
  meridiemText: string;
  /** Styles applied to the selected meridiem button element */
  selected: string;
};
```

### ClockClasses

```typescript
type ClockClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the clock element. */
  clock: string;
  /** Styles applied to the wrapper element. */
  wrapper: string;
  /** Styles applied to the square mask element. */
  squareMask: string;
  /** Styles applied to the pin element. */
  pin: string;
  /** Styles applied to the am button element. */
  amButton: string;
  /** Styles applied to the pm button element. */
  pmButton: string;
  /** Styles applied to the meridiem typography element. */
  meridiemText: string;
  /** Styles applied to the selected meridiem button element */
  selected: string;
};
```

### ClockClassKey

```typescript
type ClockClassKey =
  | 'root'
  | 'clock'
  | 'wrapper'
  | 'squareMask'
  | 'pin'
  | 'amButton'
  | 'pmButton'
  | 'meridiemText'
  | 'selected';
```

### clockNumberClasses

```typescript
type clockNumberClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to a selected root element. */
  selected: string;
  /** Styles applied to a disabled root element. */
  disabled: string;
};
```

### ClockNumberClasses

```typescript
type ClockNumberClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to a selected root element. */
  selected: string;
  /** Styles applied to a disabled root element. */
  disabled: string;
};
```

### ClockNumberClassKey

```typescript
type ClockNumberClassKey = 'root' | 'disabled' | 'selected';
```

### ClockNumberProps

```typescript
type ClockNumberProps = {
  'aria-label': string;
  disabled: boolean;
  /** Make sure callers pass an id which. It should be defined if selected. */
  id: string | undefined;
  index: number;
  inner: boolean;
  label: string;
  selected: boolean;
  classes?: Partial<ClockNumberClasses>;
};
```

### clockPointerClasses

```typescript
type clockPointerClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the thumb element. */
  thumb: string;
};
```

### ClockPointerClasses

```typescript
type ClockPointerClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the thumb element. */
  thumb: string;
};
```

### ClockPointerClassKey

```typescript
type ClockPointerClassKey = 'root' | 'thumb';
```

### ClockPointerProps

```typescript
type ClockPointerProps = {
  /**
   * `true` if the pointer is between two clock values.
   * On the `hours` view, it is always false.
   * On the `minutes` view, it is true if the pointer is on a value that is not a multiple of 5.
   */
  isBetweenTwoClockValues: boolean;
  isInner: boolean;
  type: TimeView;
  viewValue: number;
  classes?: Partial<ClockPointerClasses>;
};
```

### ClockProps

```typescript
type ClockProps = {
  ampm: boolean;
  ampmInClock: boolean;
  autoFocus?: boolean;
  children: React.ReactNode[];
  isTimeDisabled: (timeValue: number, type: TimeView) => boolean;
  minutesStep?: number;
  onChange: (value: number, isFinish?: PickerSelectionState) => void;
  /**
   * DOM id that the selected option should have
   * Should only be `undefined` on the server
   */
  selectedId: string | undefined;
  type: TimeView;
  /** The numeric value of the current view. */
  viewValue: number;
  /** The current full date value. */
  value: any | null;
  /** Minimum and maximum value of the clock. */
  viewRange: [number, number];
  className?: string;
  classes?: Partial<ClockClasses>;
  meridiemMode: Meridiem | null;
  handleMeridiemChange: (mode: 'am' | 'pm') => void;
  /**
   * If `true`, the component is disabled.
   * When disabled, the value cannot be changed and no interaction is possible.
   * @default false
   */
  disabled?: boolean;
  /**
   * If `true`, the component is read-only.
   * When read-only, the value cannot be changed but the user can interact with the interface.
   * @default false
   */
  readOnly?: boolean;
};
```

### timeClockClasses

```typescript
type timeClockClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the arrowSwitcher element. */
  arrowSwitcher: string;
};
```

### TimeClockClasses

```typescript
type TimeClockClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the arrowSwitcher element. */
  arrowSwitcher: string;
};
```

### TimeClockClassKey

```typescript
type TimeClockClassKey = 'root' | 'arrowSwitcher';
```

### TimeClockProps

```typescript
type TimeClockProps<TView extends TimeViewWithMeridiem = TimeView> = {
  /**
   * Available views.
   * @default ['hours', 'minutes']
   */
  views?: TView[];
  /** Override or extend the styles applied to the component. */
  classes?: Partial<TimeClockClasses>;
  /**
   * Overridable component slots.
   * @default {}
   */
  slots?: TimeClockSlots;
  /**
   * The props used for each component slot.
   * @default {}
   */
  slotProps?: TimeClockSlotProps;
  showViewSwitcher?: boolean;
  /**
   * Display ampm controls under the clock (instead of in the toolbar).
   * @default false
   */
  ampmInClock?: boolean;
  /**
   * If `true`, disable values before the current date for date components, time for time components and both for date time components.
   * @default false
   */
  disablePast?: boolean;
  /**
   * If `true`, disable values after the current date for date components, time for time components and both for date time components.
   * @default false
   */
  disableFuture?: boolean;
  /**
   * Minimal selectable time.
   * The date part of the object will be ignored unless `props.disableIgnoringDatePartForTimeValidation === true`.
   */
  minTime?: any;
  /**
   * Maximal selectable time.
   * The date part of the object will be ignored unless `props.disableIgnoringDatePartForTimeValidation === true`.
   */
  maxTime?: any;
  /**
   * Step over minutes.
   * @default 1
   */
  minutesStep?: number;
  /**
   * Disable specific time.
   * @returns If `true` the time will be disabled.
   */
  shouldDisableTime?: (value: any, view: TimeView) => boolean;
  /**
   * Do not ignore date part when validating min/max time.
   * @default false
   */
  disableIgnoringDatePartForTimeValidation?: boolean;
  /**
   * Choose which timezone to use for the value.
   * Example: "default", "system", "UTC", "America/New_York".
   * If you pass values from other timezones to some props, they will be converted to this timezone before being used.
   * @default The timezone of the `value` or `defaultValue` prop is defined, 'default' otherwise.
   * @see
   */
  timezone?: PickersTimezone;
  /**
   * 12h/24h view for hour selection clock.
   * @default adapter.is12HourCycleInCurrentLocale()
   */
  ampm?: boolean;
  className?: string;
  /** The system prop that allows defining system overrides as well as additional CSS styles. */
  sx?: SxProps<Theme>;
  /**
   * The selected value.
   * Used when the component is controlled.
   */
  value?: any | null;
  /**
   * The default selected value.
   * Used when the component is not controlled.
   */
  defaultValue?: any | null;
  /**
   * The date used to generate the new value when both `value` and `defaultValue` are empty.
   * @default The closest valid time using the validation props, except callbacks such as `shouldDisableTime`.
   */
  referenceDate?: any;
  /**
   * Callback fired when the value changes.
   * @template The value type. It will be the same type as `value` or `null`. It can be in `[start, end]` format in case of range value.
   * @template The view type. Will be one of date or time views.
   */
  onChange?: (
    value: any,
    selectionState?: PickerSelectionState,
    selectedView?: TimeView | 'meridiem',
  ) => void;
  /**
   * The default visible view.
   * Used when the component view is not controlled.
   * Must be a valid option from `views` list.
   */
  openTo?: TimeView | 'meridiem';
  /**
   * Callback fired on view change.
   * @template Type of the view. It will vary based on the Picker type and the `views` it uses.
   */
  onViewChange?: (view: TView) => void;
  /**
   * The visible view.
   * Used when the component view is controlled.
   * Must be a valid option from `views` list.
   */
  view?: TimeView | 'meridiem';
  /**
   * If `true`, the main element is focused during the first mount.
   * This main element is:
   * - the element chosen by the visible view if any (i.e: the selected day on the `day` view).
   * - the `input` element if there is a field rendered.
   */
  autoFocus?: boolean;
  /** Controlled focused view. */
  focusedView?: TimeView | 'meridiem' | null;
  /**
   * Callback fired on focused view change.
   * @template Type of the view. It will vary based on the Picker type and the `views` it uses.
   */
  onFocusedViewChange?: (view: TView, hasFocus: boolean) => void;
  /**
   * If `true`, the component is disabled.
   * When disabled, the value cannot be changed and no interaction is possible.
   * @default false
   */
  disabled?: boolean;
  /**
   * If `true`, the component is read-only.
   * When read-only, the value cannot be changed but the user can interact with the interface.
   * @default false
   */
  readOnly?: boolean;
};
```

### TimeClockSlotProps

```typescript
type TimeClockSlotProps = {
  previousIconButton?: SlotComponentProps<
    ExtendButtonBase<IconButtonTypeMap>,
    PickersArrowSwitcherSlotPropsOverrides,
    PickersArrowSwitcherOwnerState
  >;
  nextIconButton?: SlotComponentProps<
    ExtendButtonBase<IconButtonTypeMap>,
    PickersArrowSwitcherSlotPropsOverrides,
    PickersArrowSwitcherOwnerState
  >;
  leftArrowIcon?: SlotComponentProps<
    | (<RootComponent extends React.ElementType>(
        props: __type &
          SvgIconOwnProps &
          CommonProps &
          DistributiveOmit<
            React.ComponentPropsWithRef<RootComponent>,
            | 'style'
            | 'className'
            | 'classes'
            | 'children'
            | 'sx'
            | 'color'
            | 'fontSize'
            | 'htmlColor'
            | 'inheritViewBox'
            | 'shapeRendering'
            | 'titleAccess'
            | 'viewBox'
          >,
      ) => React.JSX.Element | null)
    | ((props: DefaultComponentProps<SvgIconTypeMap>) => React.JSX.Element | null),
    PickersArrowSwitcherSlotPropsOverrides,
    PickerOwnerState
  >;
  rightArrowIcon?: SlotComponentProps<
    | (<RootComponent extends React.ElementType>(
        props: __type &
          SvgIconOwnProps &
          CommonProps &
          DistributiveOmit<
            React.ComponentPropsWithRef<RootComponent>,
            | 'style'
            | 'className'
            | 'classes'
            | 'children'
            | 'sx'
            | 'color'
            | 'fontSize'
            | 'htmlColor'
            | 'inheritViewBox'
            | 'shapeRendering'
            | 'titleAccess'
            | 'viewBox'
          >,
      ) => React.JSX.Element | null)
    | ((props: DefaultComponentProps<SvgIconTypeMap>) => React.JSX.Element | null),
    PickersArrowSwitcherSlotPropsOverrides,
    PickerOwnerState
  >;
};
```

### TimeClockSlots

```typescript
type TimeClockSlots = {
  /**
   * Button allowing to switch to the left view.
   * @default IconButton
   */
  previousIconButton?: React.ElementType;
  /**
   * Button allowing to switch to the right view.
   * @default IconButton
   */
  nextIconButton?: React.ElementType;
  /**
   * Icon displayed in the left view switch button.
   * @default ArrowLeft
   */
  leftArrowIcon?: React.ElementType;
  /**
   * Icon displayed in the right view switch button.
   * @default ArrowRight
   */
  rightArrowIcon?: React.ElementType;
};
```

## External Types

### TimeView

```typescript
type TimeView = 'hours' | 'minutes' | 'seconds';
```

### Meridiem

```typescript
type Meridiem = 'am' | 'pm';
```
