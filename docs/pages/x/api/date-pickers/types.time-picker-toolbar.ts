import { TimePickerToolbar } from '@mui/x-date-pickers/TimePicker';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesTimePickerToolbar = createTypes(import.meta.url, TimePickerToolbar);
