# Date Pickers

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/date-pickers/types.year-calendar.md'

## API Reference

### getYearCalendarUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

### YearCalendar

Demos:

- [DateCalendar](https://mui.com/x/react-date-pickers/date-calendar/)

API:

- [YearCalendar API](https://mui.com/x/api/date-pickers/year-calendar/)

**YearCalendar Props:**

| Prop                  | Type                            | Default                                                                                              | Description                                                                                                                                                                                                                    |
| :-------------------- | :------------------------------ | :--------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| defaultValue          | `any \| null`                   | -                                                                                                    | The default selected value.&#xA;Used when the component is not controlled.                                                                                                                                                     |
| value                 | `any \| null`                   | -                                                                                                    | The selected value.&#xA;Used when the component is controlled.                                                                                                                                                                 |
| classes               | `Partial<YearCalendarClasses>`  | -                                                                                                    | Override or extend the styles applied to the component.                                                                                                                                                                        |
| disableFuture         | `boolean`                       | `false`                                                                                              | If `true`, disable values after the current date for date components, time for time components and both for date time components.                                                                                              |
| disableHighlightToday | `boolean`                       | `false`                                                                                              | If `true`, today's date is rendering without highlighting with circle.                                                                                                                                                         |
| disablePast           | `boolean`                       | `false`                                                                                              | If `true`, disable values before the current date for date components, time for time components and both for date time components.                                                                                             |
| gridLabelId           | `string`                        | -                                                                                                    | -                                                                                                                                                                                                                              |
| hasFocus              | `boolean`                       | -                                                                                                    | -                                                                                                                                                                                                                              |
| maxDate               | `any`                           | `2099-12-31`                                                                                         | Maximal selectable date.                                                                                                                                                                                                       |
| minDate               | `any`                           | `1900-01-01`                                                                                         | Minimal selectable date.                                                                                                                                                                                                       |
| onChange              | `((value: any) => void)`        | -                                                                                                    | Callback fired when the value changes.                                                                                                                                                                                         |
| onFocusedViewChange   | `((hasFocus: boolean) => void)` | -                                                                                                    | -                                                                                                                                                                                                                              |
| onYearFocus           | `((year: number) => void)`      | -                                                                                                    | -                                                                                                                                                                                                                              |
| referenceDate         | `any`                           | ``The closest valid year using the validation props, except callbacks such as `shouldDisableYear`.`` | The date used to generate the new value when both `value` and `defaultValue` are empty.                                                                                                                                        |
| shouldDisableYear     | `((year: any) => boolean)`      | -                                                                                                    | Disable specific year.                                                                                                                                                                                                         |
| slotProps             | `YearCalendarSlotProps`         | `{}`                                                                                                 | The props used for each component slot.                                                                                                                                                                                        |
| slots                 | `YearCalendarSlots`             | `{}`                                                                                                 | Overridable component slots.                                                                                                                                                                                                   |
| sx                    | `SxProps<Theme>`                | -                                                                                                    | The system prop that allows defining system overrides as well as additional CSS styles.                                                                                                                                        |
| timezone              | `PickersTimezone`               | ``The timezone of the `value` or `defaultValue` prop is defined, 'default' otherwise.``              | Choose which timezone to use for the value.&#xA;Example: "default", "system", "UTC", "America/New_York".&#xA;If you pass values from other timezones to some props, they will be converted to this timezone before being used. |
| yearsOrder            | `'asc' \| 'desc'`               | `'asc'`                                                                                              | Years are displayed in ascending (chronological) order by default.&#xA;If `desc`, years are displayed in descending order.                                                                                                     |
| yearsPerRow           | `3 \| 4`                        | `3`                                                                                                  | Years rendered per row.                                                                                                                                                                                                        |
| autoFocus             | `boolean`                       | -                                                                                                    | -                                                                                                                                                                                                                              |
| disabled              | `boolean`                       | `false`                                                                                              | If `true`, the component is disabled.&#xA;When disabled, the value cannot be changed and no interaction is possible.                                                                                                           |
| readOnly              | `boolean`                       | `false`                                                                                              | If `true`, the component is read-only.&#xA;When read-only, the value cannot be changed but the user can interact with the interface.                                                                                           |
| className             | `string`                        | -                                                                                                    | -                                                                                                                                                                                                                              |

## Additional Types

### yearCalendarClasses

```typescript
type yearCalendarClasses = Record<'root' | 'button' | 'disabled' | 'selected', string>;
```

### YearCalendarClasses

```typescript
type YearCalendarClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the button element that represents a single year */
  button: string;
  /** Styles applied to a disabled button element. */
  disabled: string;
  /** Styles applied to a selected button element. */
  selected: string;
};
```

### YearCalendarClassKey

```typescript
type YearCalendarClassKey = 'root' | 'button' | 'disabled' | 'selected';
```

### YearCalendarProps

```typescript
type YearCalendarProps = {
  autoFocus?: boolean;
  className?: string;
  /** Override or extend the styles applied to the component. */
  classes?: Partial<YearCalendarClasses>;
  /**
   * Overridable component slots.
   * @default {}
   */
  slots?: YearCalendarSlots;
  /**
   * The props used for each component slot.
   * @default {}
   */
  slotProps?: YearCalendarSlotProps;
  /** The system prop that allows defining system overrides as well as additional CSS styles. */
  sx?: SxProps<Theme>;
  /**
   * The selected value.
   * Used when the component is controlled.
   */
  value?: any | null;
  /**
   * The default selected value.
   * Used when the component is not controlled.
   */
  defaultValue?: any | null;
  /**
   * The date used to generate the new value when both `value` and `defaultValue` are empty.
   * @default The closest valid year using the validation props, except callbacks such as `shouldDisableYear`.
   */
  referenceDate?: any;
  /** Callback fired when the value changes. */
  onChange?: (value: any) => void;
  /**
   * If `true`, today's date is rendering without highlighting with circle.
   * @default false
   */
  disableHighlightToday?: boolean;
  onYearFocus?: (year: number) => void;
  hasFocus?: boolean;
  onFocusedViewChange?: (hasFocus: boolean) => void;
  gridLabelId?: string;
  /**
   * Years are displayed in ascending (chronological) order by default.
   * If `desc`, years are displayed in descending order.
   * @default 'asc'
   */
  yearsOrder?: 'asc' | 'desc';
  /**
   * Years rendered per row.
   * @default 3
   */
  yearsPerRow?: 3 | 4;
  /**
   * Disable specific year.
   * @returns If `true`, the year will be disabled.
   */
  shouldDisableYear?: (year: any) => boolean;
  /**
   * Maximal selectable date.
   * @default 2099-12-31
   */
  maxDate?: any;
  /**
   * Minimal selectable date.
   * @default 1900-01-01
   */
  minDate?: any;
  /**
   * If `true`, disable values before the current date for date components, time for time components and both for date time components.
   * @default false
   */
  disablePast?: boolean;
  /**
   * If `true`, disable values after the current date for date components, time for time components and both for date time components.
   * @default false
   */
  disableFuture?: boolean;
  /**
   * Choose which timezone to use for the value.
   * Example: "default", "system", "UTC", "America/New_York".
   * If you pass values from other timezones to some props, they will be converted to this timezone before being used.
   * @default The timezone of the `value` or `defaultValue` prop is defined, 'default' otherwise.
   * @see
   */
  timezone?: PickersTimezone;
  /**
   * If `true`, the component is disabled.
   * When disabled, the value cannot be changed and no interaction is possible.
   * @default false
   */
  disabled?: boolean;
  /**
   * If `true`, the component is read-only.
   * When read-only, the value cannot be changed but the user can interact with the interface.
   * @default false
   */
  readOnly?: boolean;
};
```

### YearCalendarSlotProps

```typescript
type YearCalendarSlotProps = {
  yearButton?: SlotComponentPropsFromProps<
    React.HTMLAttributes<HTMLButtonElement> & { sx: SxProps },
    {},
    YearButtonOwnerState
  >;
};
```

### YearCalendarSlots

```typescript
type YearCalendarSlots = {
  /**
   * Button displayed to render a single year in the `year` view.
   * @default YearCalendarButton
   */
  yearButton?: React.ElementType;
};
```
