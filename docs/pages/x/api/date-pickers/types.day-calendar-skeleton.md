# Date Pickers

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/date-pickers/types.day-calendar-skeleton.md'

## API Reference

### DayCalendarSkeleton

Demos:

- [DateCalendar](https://mui.com/x/react-date-pickers/date-calendar/)

API:

- [CalendarPickerSkeleton API](https://mui.com/x/api/date-pickers/calendar-picker-skeleton/)

**DayCalendarSkeleton Props:**

| Prop    | Type                                  | Default | Description                                                                             |
| :------ | :------------------------------------ | :------ | :-------------------------------------------------------------------------------------- |
| classes | `Partial<DayCalendarSkeletonClasses>` | -       | Override or extend the styles applied to the component.                                 |
| sx      | `SxProps<Theme>`                      | -       | The system prop that allows defining system overrides as well as additional CSS styles. |

### getDayCalendarSkeletonUtilityClass

**Parameters:**

| Parameter | Type     | Default | Description |
| :-------- | :------- | :------ | :---------- |
| slot      | `string` | -       | -           |

**Return Value:**

```tsx
type ReturnValue = string;
```

## Additional Types

### dayCalendarSkeletonClasses

```typescript
type dayCalendarSkeletonClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the week element. */
  week: string;
  /** Styles applied to the day element. */
  daySkeleton: string;
};
```

### DayCalendarSkeletonClasses

```typescript
type DayCalendarSkeletonClasses = {
  /** Styles applied to the root element. */
  root: string;
  /** Styles applied to the week element. */
  week: string;
  /** Styles applied to the day element. */
  daySkeleton: string;
};
```

### DayCalendarSkeletonClassKey

```typescript
type DayCalendarSkeletonClassKey = 'root' | 'week' | 'daySkeleton';
```

### DayCalendarSkeletonProps

```typescript
type DayCalendarSkeletonProps = {
  /** Override or extend the styles applied to the component. */
  classes?: Partial<DayCalendarSkeletonClasses>;
  /** The system prop that allows defining system overrides as well as additional CSS styles. */
  sx?: SxProps<Theme>;
  ref?: React.Ref<HTMLDivElement>;
};
```
