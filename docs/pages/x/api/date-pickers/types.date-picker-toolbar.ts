import { DatePickerToolbar } from '@mui/x-date-pickers/DatePicker';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesDatePickerToolbar = createTypes(import.meta.url, DatePickerToolbar);
