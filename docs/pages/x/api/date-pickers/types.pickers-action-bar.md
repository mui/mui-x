# Date Pickers

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/date-pickers/types.pickers-action-bar.md'

## API Reference

### PickersActionBar

**PickersActionBar Props:**

| Prop    | Type                       | Default                                                                                                                     | Description                                                                          |
| :------ | :------------------------- | :-------------------------------------------------------------------------------------------------------------------------- | :----------------------------------------------------------------------------------- |
| actions | `PickersActionBarAction[]` | ``- `[]` for Pickers with one selection step which `closeOnSelect`. - `['cancel', 'nextOrAccept']` for all other Pickers.`` | Ordered array of actions to display.&#xA;If empty, does not display that action bar. |

## Additional Types

### PickersActionBarAction

```typescript
type PickersActionBarAction = 'clear' | 'cancel' | 'accept' | 'today' | 'next' | 'nextOrAccept';
```

### PickersActionBarProps

```typescript
type PickersActionBarProps = {
  /**
 * Ordered array of actions to display.
 * If empty, does not display that action bar.
 * @default - `[]` for Pickers with one selection step which `closeOnSelect`.
- `['cancel', 'nextOrAccept']` for all other Pickers.
 */
  actions?: PickersActionBarAction[];
};
```
