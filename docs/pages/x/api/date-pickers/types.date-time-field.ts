import { DateTimeField } from '@mui/x-date-pickers/DateTimeField';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesDateTimeField = createTypes(import.meta.url, DateTimeField);
