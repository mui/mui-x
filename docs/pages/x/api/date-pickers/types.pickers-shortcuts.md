# Date Pickers

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/date-pickers/types.pickers-shortcuts.md'

## API Reference

### PickersShortcuts

Demos:

- [Shortcuts](https://mui.com/x/react-date-pickers/shortcuts/)

API:

- [PickersShortcuts API](https://mui.com/x/api/date-pickers/pickers-shortcuts/)

**PickersShortcuts Props:**

| Prop             | Type                              | Default    | Description                                                                                                                                                                                            |
| :--------------- | :-------------------------------- | :--------- | :----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| changeImportance | `PickerChangeImportance`          | `"accept"` | Importance of the change when picking a shortcut: "accept": fires `onChange`, fires `onAccept` and closes the Picker."set": fires `onChange` but do not fire `onAccept` and does not close the Picker. |
| items            | `PickersShortcutsItem<unknown>[]` | `[]`       | Ordered array of shortcuts to display.&#xA;If empty, does not display the shortcuts.                                                                                                                   |

## Additional Types

### PickersShortcutsItem

```typescript
type PickersShortcutsItem<TValue extends unknown> = {
  label: string;
  getValue: (params: PickersShortcutsItemGetValueParams<TValue>) => TValue;
  /**
   * Identifier of the shortcut.
   * If provided, it will be used as the key of the shortcut.
   */
  id?: string;
};
```

### PickersShortcutsItemContext

```typescript
type PickersShortcutsItemContext = {
  label: string;
  /**
   * Identifier of the shortcut.
   * If provided, it will be used as the key of the shortcut.
   */
  id?: string;
};
```

### PickersShortcutsProps

```typescript
type PickersShortcutsProps<TValue extends unknown> = {
  /**
   * Ordered array of shortcuts to display.
   * If empty, does not display the shortcuts.
   * @default []
   */
  items?: PickersShortcutsItem<TValue>[];
  /**
   * Importance of the change when picking a shortcut:
   * - "accept": fires `onChange`, fires `onAccept` and closes the Picker.
   * - "set": fires `onChange` but do not fire `onAccept` and does not close the Picker.
   * @default "accept"
   */
  changeImportance?: PickerChangeImportance;
};
```
