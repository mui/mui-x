import { GridAggregationFunction } from '@mui/x-data-grid-premium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesGridAggregationFunction = createTypes(import.meta.url, GridAggregationFunction);
