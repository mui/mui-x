import { GridFilterOperator } from '@mui/x-data-grid-premium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesGridFilterOperator = createTypes(import.meta.url, GridFilterOperator);
