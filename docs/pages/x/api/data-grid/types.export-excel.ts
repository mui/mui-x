import { ExportExcel } from '@mui/x-data-grid-premium/components';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesExportExcel = createTypes(import.meta.url, ExportExcel);
