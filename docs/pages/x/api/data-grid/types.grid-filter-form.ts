import { GridFilterForm } from '@mui/x-data-grid/components';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesGridFilterForm = createTypes(import.meta.url, GridFilterForm);
