import { ColumnsPanelTrigger } from '@mui/x-data-grid/components';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesColumnsPanelTrigger = createTypes(import.meta.url, ColumnsPanelTrigger);
