import { QuickFilter } from '@mui/x-data-grid/components';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesQuickFilter = createTypes(import.meta.url, QuickFilter);
