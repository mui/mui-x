import { GridFilterModel } from '@mui/x-data-grid-premium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesGridFilterModel = createTypes(import.meta.url, GridFilterModel);
