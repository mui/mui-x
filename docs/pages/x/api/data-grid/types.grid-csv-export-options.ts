import { GridCsvExportOptions } from '@mui/x-data-grid-premium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesGridCsvExportOptions = createTypes(import.meta.url, GridCsvExportOptions);
