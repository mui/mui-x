import { QuickFilterControl } from '@mui/x-data-grid/components';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesQuickFilterControl = createTypes(import.meta.url, QuickFilterControl);
