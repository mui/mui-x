import { GridExcelExportOptions } from '@mui/x-data-grid-premium';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesGridExcelExportOptions = createTypes(import.meta.url, GridExcelExportOptions);
