import { QuickFilterTrigger } from '@mui/x-data-grid/components';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesQuickFilterTrigger = createTypes(import.meta.url, QuickFilterTrigger);
