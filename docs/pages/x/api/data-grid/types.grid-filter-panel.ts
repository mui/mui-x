import { GridFilterPanel } from '@mui/x-data-grid/components';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesGridFilterPanel = createTypes(import.meta.url, GridFilterPanel);
