import { TreeItemIcon } from '@mui/x-tree-view/TreeItemIcon';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesTreeItemIcon = createTypes(import.meta.url, TreeItemIcon);
