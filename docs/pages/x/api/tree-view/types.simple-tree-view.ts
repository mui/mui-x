import { SimpleTreeView } from '@mui/x-tree-view/SimpleTreeView';
import { createTypes } from 'docsx/src/modules/api-docs/createTypes';

export const TypesSimpleTreeView = createTypes(import.meta.url, SimpleTreeView);
