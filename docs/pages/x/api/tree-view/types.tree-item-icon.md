# Tree View

[//]: types.ts '<-- Autogenerated By (do not edit the following markdown directly), run: pnpm docs:validate pages/x/api/tree-view/types.tree-item-icon.md'

## API Reference

### TreeItemIcon

**TreeItemIcon Props:**

| Prop      | Type                    | Default | Description                             |
| :-------- | :---------------------- | :------ | :-------------------------------------- |
| slotProps | `TreeItemIconSlotProps` | `{}`    | The props used for each component slot. |
| slots     | `TreeItemIconSlots`     | `{}`    | Overridable component slots.            |
| status\*  | `UseTreeItemStatus`     | -       | -                                       |

## Additional Types

### TreeItemIconProps

```typescript
type TreeItemIconProps = {
  status: UseTreeItemStatus;
  /**
   * Overridable component slots.
   * @default {}
   */
  slots?: TreeItemIconSlots;
  /**
   * The props used for each component slot.
   * @default {}
   */
  slotProps?: TreeItemIconSlotProps;
};
```

### TreeItemIconSlotProps

```typescript
type TreeItemIconSlotProps = {
  collapseIcon?: SlotComponentProps<'svg', {}, {}>;
  expandIcon?: SlotComponentProps<'svg', {}, {}>;
  endIcon?: SlotComponentProps<'svg', {}, {}>;
  icon?: SlotComponentProps<'svg', {}, {}>;
};
```

### TreeItemIconSlots

```typescript
type TreeItemIconSlots = {
  /** The icon used to collapse the item. */
  collapseIcon?: React.ElementType | null;
  /** The icon used to expand the item. */
  expandIcon?: React.ElementType | null;
  /** The icon displayed next to an end item. */
  endIcon?: React.ElementType | null;
  /** The icon to display next to the Tree Item's label. */
  icon?: React.ElementType | null;
};
```
