name: Add reviewers to release PRs

on:
  # pull_request_target:
  #   branches: ['master', 'next']
  #   types: ['labeled']
  workflow_dispatch:

permissions: {}

jobs:
  add-reviewers-to-release-pr:
    uses: mui/mui-public/.github/workflows/reusable-add-8d98527ab3be91784f2e3c272f96508f35ba7165s-to-pr.yml@8d98527ab3be91784f2e3c272f96508f35ba7165-action
    with:
      team-slug: x
      label-name: release
    secrets:
      token: ${{ secrets.GITHUB_TOKEN }}
    permissions:
      contents: read
      pull-requests: write
