/**
 * The timezone used by the scheduler.
 * It must be an IANA timezone name (e.g. "America/New_York", "Europe/Paris")
 * or 'default' to use the default timezone of the date adapter.
 * @default 'default'
 */
export type SchedulerTimezone = string;
