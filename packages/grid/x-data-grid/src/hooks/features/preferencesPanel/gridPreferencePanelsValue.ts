enum GridPreferencePanelsValue {
  filters = 'filters',
  columns = 'columns',
  // TODO v6: Avoid having a reference to aggregation on the community plan
  aggregation = 'aggregation',
}

export { GridPreferencePanelsValue };
