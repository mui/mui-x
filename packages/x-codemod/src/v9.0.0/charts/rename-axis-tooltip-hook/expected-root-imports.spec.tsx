// @ts-nocheck
import { useAxesTooltip, UseAxesTooltipReturnValue, UseAxesTooltipParams } from '@mui/x-charts';
import { useAxesTooltip as useAxisTooltipPro } from '@mui/x-charts-pro';
import { useAxesTooltip as useAxisTooltipPremium } from '@mui/x-charts-premium';

type RenameAxisTooltipParams = UseAxesTooltipParams;
type RenameAxisTooltipReturnValue = UseAxesTooltipReturnValue;

function App() {
  useAxesTooltip(params);
  useAxisTooltipPro();
  useAxisTooltipPremium();
}
