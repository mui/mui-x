// @ts-nocheck
import { useAxisTooltip, UseAxisTooltipReturnValue, UseAxisTooltipParams } from '@mui/x-charts';
import { useAxisTooltip as useAxisTooltipPro } from '@mui/x-charts-pro';

type RenameAxisTooltipParams = UseAxisTooltipParams;
type RenameAxisTooltipReturnValue = UseAxisTooltipReturnValue;

function App() {
  useAxisTooltip(params);
  useAxisTooltipPro();
}
