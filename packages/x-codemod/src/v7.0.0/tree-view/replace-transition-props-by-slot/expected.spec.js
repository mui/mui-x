<TreeItem
  slots={{
    groupTransition: Fade,
  }}
  slotProps={{
    groupTransition: { timeout: 600 },
  }} />;

<TreeItem
  slots={{
    icon: Icon,
    groupTransition: Fade,
  }}
  slotProps={{
    groupTransition: { timeout: 600 },
  }} />;
