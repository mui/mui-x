import { createLineStyleSeriesProcessor } from '@mui/x-charts/internals';

const seriesProcessor = createLineStyleSeriesProcessor<'radialLine'>('radialLine', 'Radial line');

export default seriesProcessor;
