import type { ResolvedChartExcelOptions } from './chartExcelData.types';

export const DEFAULT_CHART_EXCEL_OPTIONS: ResolvedChartExcelOptions = {
  includeHiddenSeries: true,
  includeFormattedValues: false,
  escapeFormulas: true,
};
