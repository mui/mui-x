import { EventCalendarProvider } from '../../primitives/event-calendar-provider';
import '../index.css';

/**
 * Temporary component to help rendering standalone views in the doc.
 * A clean solution will be implemented later.
 */
export const StandaloneView = EventCalendarProvider;
