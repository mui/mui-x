enum LicenseStatus {
  NotFound = 'NotFound',
  Invalid = 'Invalid',
  ExpiredAnnual = 'ExpiredAnnual',
  ExpiredVersion = 'ExpiredVersion',
  Valid = 'Valid',
  OutOfScope = 'OutOfScope',
}

export { LicenseStatus };
