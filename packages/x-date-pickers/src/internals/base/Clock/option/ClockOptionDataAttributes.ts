export enum ClockOptionDataAttributes {
  /**
   * Present when the day is selected.
   */
  selected = 'data-selected',
  /**
   * Present when the day is disabled.
   */
  disabled = 'data-disabled',
  /**
   * Present when the day is invalid.
   */
  invalid = 'data-invalid',
}
