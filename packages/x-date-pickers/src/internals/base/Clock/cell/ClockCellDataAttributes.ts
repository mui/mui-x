export enum ClockCellDataAttributes {
  /**
   * Present when the cell is selected.
   */
  selected = 'data-selected',
  /**
   * Present when the cell is disabled.
   */
  disabled = 'data-disabled',
  /**
   * Present when the cell is invalid.
   */
  invalid = 'data-invalid',
}
