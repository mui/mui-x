import * as React from 'react';
import { MonthCalendar } from '@mui/x-date-pickers/MonthCalendar';

<MonthCalendar />;

<MonthCalendar defaultValue={new Date()} />;

<MonthCalendar value={new Date()} />;

<MonthCalendar value={null} />;
