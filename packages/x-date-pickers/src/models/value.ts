import { PickerValidDate } from './pickers';

export type SimpleValue = PickerValidDate | null;

// TODO: Replace DateRange with the type once we have introduced TIsRange
// export type RangeValue = [PickerValidDate | null, PickerValidDate | null];
