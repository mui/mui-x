import type { VirtualScroller } from '@mui/x-virtualizer';

export interface GridPinnedRowsProps {
  position: 'top' | 'bottom';
  virtualScroller: VirtualScroller;
}

export function GridPinnedRows(_: GridPinnedRowsProps) {
  return null;
}
