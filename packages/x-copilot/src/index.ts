// Executor + host adapter contract
export * from './executor';

// Plugin system types + helpers
export * from './plugins';

// Generic host-agnostic copilot panel UI
export * from './components';

// Formula engine (host-agnostic — uses FormulaDataSource for row/column access)
export * from './formula-engine';
