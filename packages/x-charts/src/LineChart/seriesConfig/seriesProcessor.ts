import { createLineStyleSeriesProcessor } from '../../internals/createLineStyleSeriesProcessor';

const seriesProcessor = createLineStyleSeriesProcessor<'line'>('line', 'Line');

export default seriesProcessor;
